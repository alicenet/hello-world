/**
 * Generate a temporary account to use with MadNetWalletJS
 * Automatically adds a new burner account to the madnetwallet instance
 */
export function addBurnerAccountToMadNetWalletJS() {
    // let pRaw = new Date().valueOf()
    // let hash = await this.madWallet.Utils.hash("0x" + pRaw.toString());
    // let hash2 = await this.madWallet.Utils.hash("0x" + pRaw.toString() + pRaw.toString());
    // await this.madWallet.Account.addAccount(hash, 2)
    // await this.madWallet.Account.addAccount(hash2, 2)
    // this.gameIndex = hash
}